Description:
    Generates all files to get quickly started with MiniWiki

Example:
    ./script/generate mini_wiki

    This will create:
        Generates the required mini_wiki migration
        Adds sample configuration file
        Adds sample css file
        Adds sample layout
        Adds a sample username/authentication override module
        Adds the route to routes.rb
